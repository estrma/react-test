import React from 'react';
import WeatherApp from "../components/WeatherApp";

// TODO: tests
describe('WeatherApp', () => {
    it('fetches initial data correctly', async () => {
    });
    it('fetches location suggestions on user input', async () => {
    });
    it('sets location on suggestion selection', async () => {
    });
    it('toggles unit correctly', async () => {
    });
    it('saves last location to local storage', async () => {
    });
});
